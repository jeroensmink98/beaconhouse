#! bin/bash
echo "#######################################"
echo "Starting the postgres database"
echo "running: docker-compose up -d"
echo "#######################################"
docker-compose up -d

